.MODEL SMALL
.STACK 100H
.DATA
NEW_LINE DB 0AH,0DH,'$' 
W Dw 1,2,3,4,5   
W_END DW LABEL WORD
W_SIZE EQU W_END - W
.CODE
MAIN PROC
MOV AX, @DATA
MOV DS, AX

;LEA SI, W
;XOR AX, AX
;MOV CX, 5

;PRINT:    
;MOV AX, [SI]
;CALL OUTDEC
;ADD SI, 2
;LOOP PRINT

MOV CX, 5    
MOV SI, 0     
XOR AX, AX
PRINT_2:
ADD AX, W[SI] 
INC SI
INC SI
LOOP PRINT_2 

CALL OUTDEC
END_:
MOV AH, 4CH
INT 21H
MAIN ENDP
INCLUDE INDEC.ASM
INCLUDE OUTDEC.ASM
END MAIN


